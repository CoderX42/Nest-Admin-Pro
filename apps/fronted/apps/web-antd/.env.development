# 端口号
VITE_PORT=5173

VITE_BASE=/

# 接口地址（开发时通过 vite proxy 转发到 NestJS 后端）
VITE_GLOB_API_URL=/api

# 关闭 Nitro Mock 服务（后端接口由 NestJS 提供）
VITE_NITRO_MOCK=false

# 是否打开 devtools，true 为打开，false 为关闭
VITE_DEVTOOLS=false

# 是否注入全局loading
VITE_INJECT_APP_LOADING=true